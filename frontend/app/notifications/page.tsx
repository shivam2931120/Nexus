'use client'
import AppShell from '../../components/AppShell'
import { Bell, CheckCircle2, FileText, MessageCircle, X } from '../../components/icons'
import { useState } from 'react'
const initial = [{ text: 'Priya mentioned you in API Design Spec', meta: '12 minutes ago', icon: FileText, unread: true }, { text: 'Ravi completed “Fix auth bug”', meta: '48 minutes ago', icon: CheckCircle2, unread: true }, { text: 'New message in #frontend', meta: '2 hours ago', icon: MessageCircle, unread: false }]
export default function NotificationsPage() { const [items, setItems] = useState(initial); return <AppShell><div className="page"><div className="page-heading"><div><div className="eyebrow">INBOX</div><h1>Notifications</h1><p className="muted">One clear stream for the work that needs your attention.</p></div><button className="button" onClick={() => setItems(items.map(i => ({ ...i, unread: false })))}>Mark all read</button></div><div className="card notification-list">{items.map((item, i) => <div className={`notification-row ${item.unread ? 'unread' : ''}`} key={i}><span className="notification-icon"><item.icon size={17}/></span><div className="task-copy"><strong>{item.text}</strong><small>{item.meta}</small></div>{item.unread && <span className="status-dot progress"/>}<button className="icon-button" aria-label="Dismiss notification" onClick={() => setItems(items.filter((_, index) => index !== i))}><X size={15}/></button></div>)}{items.length === 0 && <div className="empty"><Bell size={28}/><p>You’re all caught up.</p></div>}</div></div></AppShell> }
