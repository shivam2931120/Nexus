'use client'
import AppShell from '../../components/AppShell';import {useEffect,useState} from 'react';import {CalendarDays,Plus,Video} from '../../components/icons';import {api} from '../../lib/api';import {useWorkspace,workspaceIds} from '../../lib/workspace'
type EventItem={id:string;title:string;starts_at:string;ends_at:string;kind:string};export default function CalendarPage(){const{workspace}=useWorkspace();const{orgId,teamId}=workspaceIds(workspace);const[events,setEvents]=useState<EventItem[]>([]);const[show,setShow]=useState(false);const[title,setTitle]=useState('');const[date,setDate]=useState('');useEffect(()=>{if(orgId)void api<EventItem[]>(`/orgs/${orgId}/events`).then(setEvents).catch(()=>{})},[orgId]);const create=async()=>{if(!orgId||!title.trim()||!date)return;const start=new Date(date);const end=new Date(start.getTime()+3600000);const item=await api<EventItem>(`/orgs/${orgId}/events`,{method:'POST',body:JSON.stringify({title:title.trim(),teamId,startsAt:start.toISOString(),endsAt:end.toISOString(),kind:'WORK'})});setEvents(x=>[...x,item]);setTitle('');setDate('');setShow(false)};const remove=async(id:string)=>{await api(`/events/${id}`,{method:'DELETE'});setEvents(x=>x.filter(e=>e.id!==id))};return <AppShell><div className="page"><div className="page-heading"><div><div className="eyebrow">WORKSPACE</div><h1>Calendar</h1><p className="muted">Persisted organization events and meeting schedule.</p></div><button className="button primary" onClick={()=>setShow(true)}><Plus size={15}/> New event</button></div><section className="card"><div className="card-header"><h2>Upcoming events</h2><CalendarDays size={17}/></div>{events.length===0?<div className="empty"><CalendarDays size={30}/><p>No events scheduled.</p></div>:events.map(e=><div className="task-row" key={e.id}><CalendarDays size={16} color="var(--brand)"/><div className="task-copy"><strong>{e.title}</strong><small>{new Date(e.starts_at).toLocaleString()} · {e.kind}</small></div><button className="button" onClick={()=>void remove(e.id)}>Remove</button></div>)}</section><section className="card" style={{marginTop:16}}><div className="card-header"><h2>Meetings</h2><Video size={17}/></div><a className="button" href="/meetings">Open meetings</a></section>{show&&<div className="modal-backdrop"><div className="card modal-card"><div className="card-header"><h2>New event</h2><button className="icon-button" onClick={()=>setShow(false)}>×</button></div><div className="field"><label htmlFor="event-title">Title</label><input id="event-title" autoFocus value={title} onChange={e=>setTitle(e.target.value)}/></div><div className="field"><label htmlFor="event-date">Date and time</label><input id="event-date" type="datetime-local" value={date} onChange={e=>setDate(e.target.value)}/></div><button className="button primary" onClick={()=>void create()}>Create event</button></div></div>}</div></AppShell>}
