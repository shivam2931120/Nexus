'use client'
import AppShell from '../../components/AppShell'; import {useState} from 'react'; import {MessageCircle,Paperclip,Plus,Search,Users} from '../../components/icons';
const seed=[{name:'Priya Sharma',initial:'P',color:'#8b5cf6',text:'The API contract is ready for review. I left a few notes in the document.',time:'9:42 AM'},{name:'Ravi Mehta',initial:'R',color:'#0ea5e9',text:'Looks good. I’ll take the auth edge cases today.',time:'9:46 AM'},{name:'Neha Kapoor',initial:'N',color:'#f97316',text:'I’ve added the onboarding checklist to the shared docs.',time:'10:02 AM'}];
export default function Chat(){const [messages,setMessages]=useState(seed);const [text,setText]=useState('');function send(){if(!text.trim())return;setMessages([...messages,{name:'You',initial:'S',color:'#6366f1',text,time:'now'}]);setText('')}return <AppShell><div className="chat-layout"><aside className="chat-list"><div className="card-header"><h2>Messages</h2><button className="icon-button" aria-label="New channel"><Plus size={16}/></button></div><div className="search" style={{marginBottom:14}}><Search size={14}/><span>Find a channel</span></div>{['# general','# frontend','# product updates'].map((channel,i)=><div className={`nav-item ${i===1?'active':''}`} key={channel}><MessageCircle size={15}/><span>{channel}</span>{i===0&&<b className="nav-count">2</b>}</div>)}<div className="nav-section">DIRECT MESSAGES</div>{['Priya Sharma','Ravi Mehta'].map(name=><div className="nav-item" key={name}><span className="avatar" style={{width:22,height:22,background:'#8b5cf6'}}>{name[0]}</span><span>{name}</span></div>)}</aside><section className="chat-main"><header className="chat-header"><div style={{display:'flex',alignItems:'center',justifyContent:'space-between'}}><div><h2># frontend</h2><span className="muted" style={{fontSize:12}}>42 members · Engineering</span></div><div className="actions"><button className="icon-button" aria-label="Search messages"><Search size={17}/></button><button className="icon-button" aria-label="Members"><Users size={17}/></button></div></div></header><div className="messages"><div className="eyebrow" style={{textAlign:'center',marginBottom:26}}>TODAY</div>{messages.map((m,i)=><div className="message" key={`${m.name}-${i}`}><span className="avatar" style={{background:m.color}}>{m.initial}</span><div className="message-body"><div className="message-meta"><strong>{m.name}</strong><time>{m.time}</time></div><p>{m.text}</p></div></div>)}</div><form className="composer" onSubmit={e=>{e.preventDefault();send()}}><button type="button" className="icon-button" aria-label="Attach file"><Paperclip size={17}/></button><input value={text} onChange={e=>setText(e.target.value)} placeholder="Message #frontend" aria-label="Message"/><button className="button primary" type="submit">Send</button></form></section></div></AppShell>}
