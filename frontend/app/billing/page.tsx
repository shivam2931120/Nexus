'use client'
import AppShell from '../../components/AppShell'
import { CheckCircle2, CreditCard, Sparkles } from '../../components/icons'
const plans = [{ name: 'Free', price: '$0', description: 'For small teams getting started', features: ['Up to 10 members', '5 GB storage', 'Chat, tasks, basic docs'] }, { name: 'Starter', price: '$9', description: 'For growing teams that need more', features: ['Everything in Free', 'Meetings and calendar', '50 GB storage'] }, { name: 'Business', price: '$19', description: 'For teams scaling their workspace', features: ['Everything in Starter', 'Analytics and automation', 'Advanced permissions'] }]
export default function BillingPage() { return <AppShell><div className="page"><div className="page-heading"><div><div className="eyebrow">WORKSPACE SETTINGS</div><h1>Plans & billing</h1><p className="muted">Choose the plan that fits how your team works.</p></div><button className="button"><CreditCard size={15}/> Manage payment method</button></div><div className="plan-grid">{plans.map((plan, i) => <article className={`card plan-card ${i === 1 ? 'featured' : ''}`} key={plan.name}>{i === 1 && <span className="plan-label"><Sparkles size={13}/> Most popular</span>}<h2>{plan.name}</h2><p className="muted">{plan.description}</p><div className="plan-price">{plan.price}<small>/user/month</small></div>{plan.features.map(f => <div className="feature-line" key={f}><CheckCircle2 size={15} color="var(--brand)"/>{f}</div>)}<button className={`button ${i === 1 ? 'primary' : ''}`} style={{ width: '100%', justifyContent: 'center', marginTop: 18 }}>{i === 0 ? 'Current plan' : 'Choose plan'}</button></article>)}</div></div></AppShell> }
