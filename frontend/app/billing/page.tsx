'use client'
import AppShell from '../../components/AppShell';import {CreditCard} from '../../components/icons';
export default function BillingPage(){return <AppShell><div className="page"><div className="page-heading"><div><div className="eyebrow">WORKSPACE SETTINGS</div><h1>Billing</h1><p className="muted">Payments are intentionally disabled until a payment model is selected.</p></div><CreditCard size={22}/></div><section className="card" style={{maxWidth:720}}><h2>No recurring subscriptions</h2><p className="muted">Nexus is not using Razorpay subscriptions. Your existing Razorpay API keys remain unused until you choose a one-time payment flow, such as invoices or meeting credits.</p><div className="form-error" style={{color:'var(--text)',background:'var(--brand-soft)',borderColor:'var(--brand)'}}>No payment action is available yet, so users cannot accidentally create a subscription.</div></section></div></AppShell>}
