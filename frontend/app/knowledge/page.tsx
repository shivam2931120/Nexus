'use client'
import AppShell from '../../components/AppShell'
import { BookOpen, ChevronRight, FileText, Plus, Search } from '../../components/icons'
import { useState } from 'react'
const pages = [{ title: 'Engineering handbook', children: ['Getting started', 'Development workflow', 'Release process'] }, { title: 'Policies', children: ['Security policy', 'Leave policy', 'Code of conduct'] }, { title: 'Technical documentation', children: ['Architecture', 'API standards', 'Runbooks'] }]
export default function KnowledgePage() { const [selected, setSelected] = useState('Engineering handbook'); return <AppShell><div className="page"><div className="page-heading"><div><div className="eyebrow">KNOWLEDGE BASE</div><h1>Knowledge Base</h1><p className="muted">Make the team’s best context easy to find and keep current.</p></div><button className="button primary"><Plus size={15}/> New page</button></div><div className="knowledge-layout"><aside className="card"><div className="search" style={{ marginBottom: 16, background: 'var(--surface)' }}><Search size={15}/><span>Search knowledge</span></div>{pages.map(page => <div key={page.title}><button className={`nav-item ${selected === page.title ? 'active' : ''}`} style={{ width: '100%' }} onClick={() => setSelected(page.title)}><BookOpen size={15}/><span>{page.title}</span><ChevronRight size={14}/></button>{page.children.map(child => <button className="nav-item nested" key={child} onClick={() => setSelected(child)}><FileText size={14}/><span>{child}</span></button>)}</div>)}</aside><article className="card knowledge-article"><div className="eyebrow">ENGINEERING · UPDATED TODAY</div><h1>{selected}</h1><p className="muted">A shared page for decisions, guidance, and links that help everyone move with confidence.</p><hr/><h2>Overview</h2><p>Welcome to the Nexus knowledge base. Keep this space practical: explain why a decision exists, link to the source of truth, and assign an owner for anything that can become stale.</p><h2>Related resources</h2><div className="resource-row"><FileText size={16} color="var(--brand)"/><span>API Design Spec</span><ChevronRight size={15} className="muted"/></div><div className="resource-row"><FileText size={16} color="var(--brand)"/><span>Onboarding checklist</span><ChevronRight size={15} className="muted"/></div></article></div></div></AppShell> }
