'use client'
import AppShell from '../../components/AppShell'; import { useEffect, useState } from 'react'
export default function Settings(){const [dark,setDark]=useState(false);useEffect(()=>setDark(localStorage.getItem('nexus_theme')==='dark'),[]);const toggle=()=>{const next=!dark;setDark(next);localStorage.setItem('nexus_theme',next?'dark':'light');document.documentElement.dataset.theme=next?'dark':'light'};return <AppShell><div className="page"><div className="eyebrow">ACCOUNT</div><h1>Settings</h1><div className="card" style={{marginTop:24,maxWidth:720}}><h2>Appearance</h2><p className="muted">Choose the theme for this workspace. You can also use the header control.</p><button className="button" type="button" onClick={toggle}>{dark?'Switch to light mode':'Switch to dark mode'}</button></div><div className="card" style={{marginTop:16,maxWidth:720}}><h2>Profile</h2><p className="muted">Your identity and account security are managed by Clerk.</p><a className="button" href="/login">Open account security</a></div></div></AppShell>}
