import AppShell from '../../components/AppShell'
import { CalendarDays, CheckCircle2, Github, MessageCircle, Plus, Video } from '../../components/icons'
export default function IntegrationsPage() { const integrations = [['Google Calendar','Sync events and availability','Connected',CalendarDays],['GitHub','Link pull requests and issues','Connect',Github],['LiveKit','Video rooms and recordings','Connect',Video],['Slack','Bring notifications into one inbox','Connect',MessageCircle]] as const; return <AppShell><div className="page"><div className="page-heading"><div><div className="eyebrow">WORKSPACE SETTINGS</div><h1>Integrations</h1><p className="muted">Connect the tools your team already relies on.</p></div><button className="button primary"><Plus size={15}/> Browse integrations</button></div><div className="integration-grid">{integrations.map(([name,description,status,Icon]) => <article className="card integration-card" key={name}><div className="integration-icon"><Icon size={22}/></div><div><h2>{name}</h2><p className="muted">{description}</p></div><div className="integration-footer">{status === 'Connected' ? <span className="connected"><CheckCircle2 size={14}/> Connected</span> : <button className="button">Connect</button>}</div></article>)}</div></div></AppShell> }
