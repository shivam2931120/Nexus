import AppShell from '../../components/AppShell'
import { CalendarDays, Github, MessageCircle, Plus, Video } from '../../components/icons'
export default function IntegrationsPage() { const integrations = [['Google Calendar','Sync events and availability','Ready to connect',CalendarDays],['GitHub','Link pull requests and issues','Not configured',Github],['LiveKit','Video rooms and recordings','Configured for meetings',Video],['Slack','Bring notifications into one inbox','Not configured',MessageCircle]] as const; return <AppShell><div className="page"><div className="page-heading"><div><div className="eyebrow">WORKSPACE SETTINGS</div><h1>Integrations</h1><p className="muted">Connect the tools your team already relies on.</p></div><button className="button primary" type="button"><Plus size={15}/> Browse integrations</button></div><div className="integration-grid">{integrations.map(([name,description,status,Icon]) => <article className="card integration-card" key={name}><div className="integration-icon"><Icon size={22}/></div><div><h2>{name}</h2><p className="muted">{description}</p></div><div className="integration-footer"><span className={status === 'Configured for meetings' ? 'connected' : 'muted'}>{status}</span></div></article>)}</div></div></AppShell> }
