'use client'
import AppShell from '../../components/AppShell'; import { useState } from 'react'; import { Search, UserPlus, Users } from '../../components/icons'
export default function DirectoryPage(){const [query,setQuery]=useState('');const [show,setShow]=useState(false);const [email,setEmail]=useState('');const [invites,setInvites]=useState<string[]>([]);const invite=()=>{if(!email.trim())return;setInvites(i=>[...i,email.trim()]);setEmail('');setShow(false)};return <AppShell><div className="page"><div className="page-heading"><div><div className="eyebrow">PEOPLE</div><h1>Directory</h1><p className="muted">Manage your workspace members and invitations.</p></div><button className="button primary" type="button" onClick={()=>setShow(true)}><UserPlus size={15}/> Invite member</button></div><div className="directory-toolbar card"><div className="search" style={{maxWidth:420,background:'var(--surface)'}}><Search size={15}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search people" style={{border:0,outline:0,background:'transparent',color:'var(--text)',width:'100%'}}/></div></div><div className="card empty"><Users size={32}/><p>{query?'No members match your search.':'No members have joined this workspace yet.'}</p><button className="button primary" type="button" onClick={()=>setShow(true)}><UserPlus size={15}/> Invite your team</button></div>{invites.length>0&&<div className="card" style={{marginTop:16}}><h2>Pending invitations</h2>{invites.map(item=><div className="task-row" key={item}><UserPlus size={16}/><div className="task-copy"><strong>{item}</strong><small>Invitation pending</small></div></div>)}</div>}{show&&<div className="modal-backdrop"><div className="card modal-card"><div className="card-header"><h2>Invite member</h2><button className="icon-button" type="button" onClick={()=>setShow(false)}>×</button></div><div className="field"><label htmlFor="invite-email">Work email</label><input id="invite-email" autoFocus type="email" value={email} onChange={e=>setEmail(e.target.value)} onKeyDown={e=>e.key==='Enter'&&invite()} placeholder="person@company.com"/></div><button className="button primary" type="button" onClick={invite}>Send invitation</button></div></div>}</div></AppShell>}
