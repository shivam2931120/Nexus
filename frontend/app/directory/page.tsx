'use client'
import AppShell from '../../components/AppShell'
import { MapPin, Search, UserPlus, Users } from '../../components/icons'
import { useState } from 'react'
const people = [{ name: 'Shivam Kumar', role: 'Product lead', team: 'Engineering', location: 'Bengaluru', color: '#6366f1' }, { name: 'Priya Sharma', role: 'Project manager', team: 'Engineering', location: 'Mumbai', color: '#8b5cf6' }, { name: 'Ravi Mehta', role: 'Senior engineer', team: 'Engineering', location: 'Delhi', color: '#0ea5e9' }, { name: 'Neha Kapoor', role: 'People operations', team: 'People', location: 'Pune', color: '#f97316' }]
export default function DirectoryPage() { const [query, setQuery] = useState(''); return <AppShell><div className="page"><div className="page-heading"><div><div className="eyebrow">PEOPLE</div><h1>Directory</h1><p className="muted">Find the right teammate and understand how the organization connects.</p></div><button className="button primary"><UserPlus size={15}/> Invite member</button></div><div className="directory-toolbar card"><div className="search" style={{ maxWidth: 420, background: 'var(--surface)' }}><Search size={15}/><input value={query} onChange={e => setQuery(e.target.value)} placeholder="Search people, roles, or teams" style={{ border: 0, outline: 0, background: 'transparent', color: 'var(--text)', width: '100%' }}/></div><div className="actions"><button className="button">All teams</button><button className="button">All locations</button></div></div><div className="directory-grid">{people.filter(p => `${p.name} ${p.role} ${p.team}`.toLowerCase().includes(query.toLowerCase())).map(person => <article className="card person-card" key={person.name}><div className="person-top"><span className="avatar large" style={{ background: person.color }}>{person.name.split(' ').map(n => n[0]).join('')}</span><span className="status-online" title="Available"/></div><h2>{person.name}</h2><p>{person.role}</p><span className="badge">{person.team}</span><small className="muted"><MapPin size={13}/> {person.location}</small></article>)}</div><section className="card org-chart"><div className="card-header"><h2>Organization chart</h2><Users size={17} className="muted"/></div><div className="org-node root">Shivam Kumar<small>Product lead</small></div><div className="org-connector"/><div className="org-children">{people.slice(1,4).map(p => <div className="org-node" key={p.name}>{p.name}<small>{p.role}</small></div>)}</div></section></div></AppShell> }
