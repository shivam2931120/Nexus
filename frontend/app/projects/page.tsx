'use client'
import AppShell from '../../components/AppShell'; import { useState } from 'react'; import { Plus, FolderKanban } from '../../components/icons'
export default function Projects(){const [projects,setProjects]=useState<string[]>([]);const [show,setShow]=useState(false);const [name,setName]=useState('');const create=()=>{if(!name.trim())return;setProjects(p=>[...p,name.trim()]);setName('');setShow(false)};return <AppShell><div className="page"><div className="page-heading"><div><div className="eyebrow">WORKSPACE</div><h1>Projects</h1><p className="muted">Create and organize your team’s work.</p></div><button className="button primary" type="button" onClick={()=>setShow(true)}><Plus size={15}/> New project</button></div>{projects.length===0?<div className="card empty"><FolderKanban size={32}/><p>No projects yet.</p><button className="button primary" type="button" onClick={()=>setShow(true)}><Plus size={15}/> Create project</button></div>:<div className="grid dashboard-grid">{projects.map(project=><div className="card" key={project}><h2>{project}</h2><p className="muted">New project</p><div className="progress" style={{margin:'18px 0 10px'}}><i style={{width:'0%'}}/></div><span className="muted" style={{fontSize:12}}>0% complete</span></div>)}</div>}{show&&<div className="modal-backdrop"><div className="card modal-card"><div className="card-header"><h2>New project</h2><button className="icon-button" type="button" onClick={()=>setShow(false)}>×</button></div><div className="field"><label htmlFor="project-name">Project name</label><input id="project-name" autoFocus value={name} onChange={e=>setName(e.target.value)} onKeyDown={e=>e.key==='Enter'&&create()} placeholder="Website launch"/></div><button className="button primary" type="button" onClick={create}>Create project</button></div></div>}</div></AppShell>}
