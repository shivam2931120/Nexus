'use client'
import AppShell from '../../components/AppShell';import {useEffect,useState} from 'react';import {Plus,FolderKanban} from '../../components/icons';import {api} from '../../lib/api';import {useWorkspace,workspaceIds} from '../../lib/workspace'
type Project={id:string;name:string;description:string};export default function Projects(){const {workspace}=useWorkspace();const {orgId,teamId}=workspaceIds(workspace);const [items,setItems]=useState<Project[]>([]);const[show,setShow]=useState(false);const[name,setName]=useState('');useEffect(()=>{if(orgId)void api<Project[]>(`/orgs/${orgId}/projects`).then(setItems).catch(()=>{})},[orgId]);const create=async()=>{if(!name.trim())return;const item=await api<Project>(`/orgs/${orgId}/projects`,{method:'POST',body:JSON.stringify({name:name.trim(),teamId})});setItems(x=>[item,...x]);setName('');setShow(false)};return <AppShell><div className="page"><div className="page-heading"><div><div className="eyebrow">WORKSPACE</div><h1>Projects</h1><p className="muted">Create and organize your team’s work.</p></div><button className="button primary" onClick={()=>setShow(true)}><Plus size={15}/> New project</button></div>{items.length===0?<div className="card empty"><FolderKanban size={32}/><p>No projects yet.</p><button className="button primary" onClick={()=>setShow(true)}><Plus size={15}/> Create project</button></div>:<div className="grid dashboard-grid">{items.map(p=><div className="card" key={p.id}><h2>{p.name}</h2><p className="muted">{p.description||'No description'}</p></div>)}</div>}{show&&<div className="modal-backdrop"><div className="card modal-card"><div className="card-header"><h2>New project</h2><button className="icon-button" onClick={()=>setShow(false)}>×</button></div><div className="field"><label htmlFor="project-name">Project name</label><input id="project-name" autoFocus value={name} onChange={e=>setName(e.target.value)} /></div><button className="button primary" onClick={()=>void create()}>Create project</button></div></div>}</div></AppShell>}
