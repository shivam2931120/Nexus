'use client'
import AppShell from '../components/AppShell'; import {CalendarDays,CheckCircle2,FileText,Plus,Video} from '../components/icons';
const tasks=[['Fix auth bug','Due today · High','progress'],['Review API design','Due tomorrow · Medium',''],['Prepare sprint demo','Due Friday · Low','']]; const docs=[['Sprint 12 Notes','Edited 2 hours ago'],['API Design Spec','Edited yesterday'],['Onboarding Wiki','Edited Aug 1']];
export default function Home(){return <AppShell><div className="page"><div className="page-heading"><div><div className="eyebrow">TUESDAY, AUGUST 5</div><h1>Good morning, Shivam <span aria-hidden>👋</span></h1><p className="muted">Here’s what’s happening across your workspace.</p></div><div className="actions"><button className="button primary"><Plus size={15}/> New task</button><button className="button"><FileText size={15}/> New doc</button><button className="button"><Video size={15}/> Start meeting</button></div></div><div className="grid dashboard-grid"><section className="card"><div className="card-header"><h2>My tasks <span className="muted">(5)</span></h2><a className="muted" href="/tasks" style={{fontSize:12}}>View all</a></div>{tasks.map(([title,meta,state])=><div className="task-row" key={title}><span className={`status-dot ${state}`}></span><div className="task-copy"><strong>{title}</strong><small>{meta}</small></div><span className="badge">{state==='progress'?'In progress':'To do'}</span></div>)}<div className="empty" style={{paddingBottom:0}}>+ 2 more tasks</div></section><section className="card"><div className="card-header"><h2>Today’s meetings</h2><CalendarDays size={17} className="muted"/></div><div className="card" style={{background:'var(--brand-soft)',border:0}}><div style={{display:'flex',gap:10}}><Video size={18} color="var(--brand)"/><div><strong style={{fontSize:13}}>Sprint standup</strong><p className="muted" style={{fontSize:12,margin:'4px 0 12px'}}>10:00 AM · 30 min · Engineering</p><button className="button primary">Join meeting</button></div></div></div><div className="empty">No other meetings today.</div></section><section className="card"><div className="card-header"><h2>Recent documents</h2><FileText size={17} className="muted"/></div>{docs.map(([title,meta])=><div className="doc-row" key={title}><FileText size={17} color="var(--brand)"/><div className="doc-copy"><strong>{title}</strong><small>{meta}</small></div></div>)}</section><section className="card"><div className="card-header"><h2>Team activity</h2><CheckCircle2 size={17} className="muted"/></div>{[['Priya','updated API Design Spec','12 min ago'],['Ravi','completed Task #56','48 min ago'],['Neha','joined Engineering','2 hours ago']].map(([who,what,when])=><div className="activity-row" key={who}><span className="avatar" style={{background:'#0ea5e9'}}>{who[0]}</span><div><span style={{fontSize:13}}><strong>{who}</strong> {what}</span><small>{when}</small></div></div>)}</section></div><section className="card" style={{marginTop:16}}><div className="card-header"><h2>Project progress</h2><a className="muted" href="/projects" style={{fontSize:12}}>View projects</a></div>{[['Nexus MVP','78%','78%','Due Aug 30'],['Marketing site','42%','42%','Due Sep 15']].map(([name,value,width,due])=><div className="project-progress" key={name}><div className="project-line"><span>{name}</span><span className="muted">{value} · {due}</span></div><div className="progress"><i style={{width}}/></div></div>)}</section></div></AppShell>}
