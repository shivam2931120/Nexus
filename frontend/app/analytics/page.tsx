import AppShell from '../../components/AppShell'
import { Activity, BarChart3, CheckCircle2, MessageCircle, TrendingUp } from '../../components/icons'
export default function AnalyticsPage() { const bars = [36, 52, 43, 68, 61, 82, 74, 91, 78, 88, 95, 86]; const metrics = [{ label: 'Active members', value: '42', change: '+12.5%', Icon: UsersIcon }, { label: 'Messages sent', value: '8,492', change: '+18.2%', Icon: MessageCircle }, { label: 'Tasks completed', value: '186', change: '+8.4%', Icon: CheckCircle2 }, { label: 'Avg. session', value: '46m', change: '+4.1%', Icon: TrendingUp }]; return <AppShell><div className="page"><div className="page-heading"><div><div className="eyebrow">INSIGHTS</div><h1>Analytics</h1><p className="muted">A clear view of how your organization is communicating and shipping.</p></div><button className="button">Last 30 days ▾</button></div><div className="metric-grid">{metrics.map(({ label, value, change, Icon }) => <div className="card metric-card" key={label}><Icon size={18} color="var(--brand)"/><span className="muted">{label}</span><strong>{value}</strong><small>{change} vs previous period</small></div>)}</div><div className="grid dashboard-grid" style={{ marginTop: 16 }}><section className="card"><div className="card-header"><div><h2>Workspace activity</h2><p className="muted" style={{ fontSize: 12 }}>Messages and actions by day</p></div><Activity size={18} className="muted"/></div><div className="bar-chart">{bars.map((height, i) => <div className="bar-column" key={i}><i style={{ height: `${height}%` }}/><small>{i + 1}</small></div>)}</div></section><section className="card"><div className="card-header"><h2>Project health</h2><BarChart3 size={18} className="muted"/></div>{[['Nexus MVP','78%','On track'],['Marketing site','42%','Needs attention'],['Onboarding','91%','On track']].map(([name,progress,status]) => <div className="project-progress" key={name}><div className="project-line"><span>{name}</span><span className="muted">{progress}</span></div><div className="progress"><i style={{ width: progress }}/></div><small className="muted">{status}</small></div>)}</section></div></div></AppShell> }
function UsersIcon(props: { size?: number; color?: string }) { return <span {...props} style={{ display: 'inline-block', width: props.size ?? 18, height: props.size ?? 18, border: `2px solid ${props.color ?? 'currentColor'}`, borderRadius: '50%' }} /> }
