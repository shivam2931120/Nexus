import AppShell from '../../components/AppShell'
import { Activity, BarChart3, CheckCircle2, MessageCircle, TrendingUp } from '../../components/icons'
export default function AnalyticsPage() { const metrics = [{ label: 'Active members', value: '0', Icon: UsersIcon }, { label: 'Messages sent', value: '0', Icon: MessageCircle }, { label: 'Tasks completed', value: '0', Icon: CheckCircle2 }, { label: 'Avg. session', value: '—', Icon: TrendingUp }]; return <AppShell><div className="page"><div className="page-heading"><div><div className="eyebrow">INSIGHTS</div><h1>Analytics</h1><p className="muted">Workspace metrics will populate as your team uses Nexus.</p></div><button className="button" type="button">Last 30 days ▾</button></div><div className="metric-grid">{metrics.map(({ label, value, Icon }) => <div className="card metric-card" key={label}><Icon size={18} color="var(--brand)"/><span className="muted">{label}</span><strong>{value}</strong><small className="muted">No activity recorded yet</small></div>)}</div><div className="grid dashboard-grid" style={{ marginTop: 16 }}><section className="card empty"><Activity size={28}/><h2>Workspace activity</h2><p>Charts will appear after your workspace has activity.</p></section><section className="card empty"><BarChart3 size={28}/><h2>Project health</h2><p>Create projects and tasks to see progress.</p></section></div></div></AppShell> }
function UsersIcon(props: { size?: number; color?: string }) { return <span {...props} style={{ display: 'inline-block', width: props.size ?? 18, height: props.size ?? 18, border: `2px solid ${props.color ?? 'currentColor'}`, borderRadius: '50%' }} /> }
